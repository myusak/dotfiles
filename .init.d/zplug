## vim:ft=zsh

function install_zplug() {
	if [ ! -e $HOME/.zplug ]; then
		echo "Install zplug"
		curl -sL zplug.sh/installer | zsh
	fi
}

function remove_zplug() {
	if [ -e $HOME/.zplug ]; then
		echo "Remove zplug"
		rm -rf $HOME/.zplug
	fi
}
